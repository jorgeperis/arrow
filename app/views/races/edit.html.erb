<% content_for :title, "Editing race" %>

<h1 class="font-bold text-4xl mbe-4">Editing race</h1>

<%= render "form", race: @race %>

<div class="inline-flex flex-wrap items-center gap mbs-2" style="--row-gap: .5rem">
  <%= link_to "Show this race", @race, class: "btn" %>
  <%= link_to "Back to races", races_path, class: "btn" %>
</div>
