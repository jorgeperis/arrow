<% content_for :title, "New goal" %>

<h1 class="font-bold text-4xl mbe-4">New goal</h1>

<div class="card">
  <%= render "form", goal: @goal %>
</div>

<div class="inline-flex flex-wrap items-center gap mbs-2" style="--row-gap: .5rem">
  <%= link_to "Back to goals", goals_path, class: "btn" %>
</div>
